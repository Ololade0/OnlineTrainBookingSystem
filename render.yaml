services:
  - type: web
    name: train-booking-app
    env: docker
    plan: free
    autoDeploy: true

  - type: pserv
    name: activemq
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile.activemq
    autoDeploy: false
