# Use official ActiveMQ image
FROM rmohr/activemq:5.17.6

# Set admin credentials
ENV ACTIVEMQ_ADMIN_LOGIN=admin
ENV ACTIVEMQ_ADMIN_PASSWORD=admin

# Optional: expose ports (Render handles this from render.yaml too)
EXPOSE 61616 8161
